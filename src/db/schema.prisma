datasource db {
   provider = "sqlite"
   url      = "file:./dev.db"
}

generator kysely {
   provider  = "prisma-kysely"
   output    = "./"
   fileName  = "types.ts"
   camelCase = true
}

// IMPORTANT: Ensure that table models always map to a table name that is lowercase and snake_case and is plural
